Module Company
    'Public CName As String = "Bisma Textile Mills Limited"
    Public CName As String = "Bisma Textile Mills Limited"
    Public CName1 As String = ""
    Public CAddress As String = "Warburton Road, Distt. Sheikhupura"
    Public CPhone As String = ""
    Public AppName As String = "GPMS"

    Public Sub GetInfo()
        'Dim D As New DAL.DAL
        'Dim N As String
        'Dim A As String
        'Dim P As String
        'N = D.GetScaler("Select SettingValue from tblSetting where SettingName ='N'")
        'A = D.GetScaler("Select SettingValue from tblSetting where SettingName ='A'")
        'P = D.GetScaler("Select SettingValue from tblSetting where SettingName ='P'")
        'CName = Encrypt.EncryptStr(N)
        'CAddress = Encrypt.EncryptStr(A)
        'CPhone = Encrypt.EncryptStr(P)
    End Sub
End Module
